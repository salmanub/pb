Wraps a form control with a mono uppercase label and optional hint. Compose with `Input` / `Textarea`.

```jsx
<Field label="Correo" htmlFor="email" hint="Te responderemos en 24 h laborables.">
  <Input id="email" type="email" placeholder="nombre@empresa.com" />
</Field>
```
