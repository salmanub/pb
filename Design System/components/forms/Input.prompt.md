Single-line input or (with `multiline`) a textarea. Hairline border, ink border + oxide ring on focus.

```jsx
<Input type="text" placeholder="Población del inmueble" />
<Input multiline rows={5} placeholder="Describe brevemente el problema…" />
```

`invalid` turns the border oxide for error states.
