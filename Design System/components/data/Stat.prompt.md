A single verifiable figure — big mono number, sober label, optional source note. Use these to substantiate authority with data instead of claims. Always cite a `source` where possible.

```jsx
<Stat value="€2,8 M" label="Mayor importe peritado en un único dictamen" source="Expediente 22-074" />
<Stat value="100%" label="Dictámenes ratificados en sede judicial" accent />
```

`align` left or center; `accent` colors the figure oxide.
