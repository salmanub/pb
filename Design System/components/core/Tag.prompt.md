Monospace, tracked label — the brand's signature device. Use for section eyebrows, reference codes (`EXP. 16448`), case status, and figure sources. Always uppercase mono.

```jsx
<Tag variant="accent" dot>Ratificado en sala</Tag>
<Tag variant="outline">§ 02 · Servicios</Tag>
```

Variants: `default`, `outline`, `accent` (oxide tint), `ink` (solid dark), `positive`, `caution`. Sizes `sm | md`. `dot` adds a leading status dot.
