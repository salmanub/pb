Documentary surface with a hairline border and near-square corners — no glossy shadow. Pass `refCode` for the signature mono header rule; `interactive` for hover lift on linked cards.

```jsx
<Card refCode="EXP. 24-118 · Tarragona" interactive>
  <div style={{ padding: 20 }}>…</div>
</Card>
```

Tones: `paper` (default), `raised` (white), `ink` (dark surface), `tint` (oxide). Add your own inner padding.
