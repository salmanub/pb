---
layout: layouts/post.njk
pagination:
  data: posts_caPublished
  size: 1
  alias: post
  addAllPagesToCollections: true
eleventyComputed:
  permalink: "{{ '/ca/blog/' + post.slug + '/' }}"
  title: "{{ post.title }}"
  description: "{{ post.description }}"
  lang: ca
---
