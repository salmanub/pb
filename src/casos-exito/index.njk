---
layout: layouts/base.njk
title: "Casos de Éxito · Informes Periciales Resueltos"
description: "Selección de expedientes periciales reales en edificación, obra civil y seguros. Patologías documentadas, cuantías peritadas y resultados obtenidos para nuestros clientes."
image: "auditoria-informe-tecnico.jpg"
image_alt: "Perito revisando documentación de casos de éxito en Barcelona"
lang: es
pagination:
  data: casosExitoPub
  size: 6
  alias: casosPage
  addAllPagesToCollections: true
translations:
  - lang: ca
    permalink: /ca/casos-d-exit/
  - lang: en
    permalink: /en/success-cases/
permalink: "/casos-exito/{% if pagination.pageNumber > 0 %}pagina/{{ pagination.pageNumber + 1 }}/{% endif %}"
eleventyComputed:
  eleventyNavigation:
    key: "{{ 'Casos' if pagination.pageNumber == 0 else '' }}"
    order: 3
---
{# casos-exito/index.njk — Listado DS-pixel-fiel con paginación #}
{% set ui = es %}
{% set langPrefix = "" %}

{# ── Breadcrumb inline vía partial ── #}
{% set breadcrumb_inline = true %}
{% set breadcrumb_items = [
  { label: "Inicio", url: "/" },
  { label: "Casos" }
] %}
{% include "partials/breadcrumb.njk" %}
{% set breadcrumb_inline = false %}

{# ── PageHero (DS: serif display, lede 2 líneas) ── #}
<section class="page-hero">
  <div class="page-hero__eyebrow" aria-hidden="true">
    <span class="page-hero__eyebrow-line"></span>
    § 04 · Expedientes ratificados
  </div>
  <h1 class="page-hero__h1">Casos de éxito<br>en informes periciales</h1>
  <p class="page-hero__lede">Selección de expedientes periciales reales en edificación, obra civil y seguros. Patologías documentadas, cuantías peritadas y resultados obtenidos para nuestros clientes.</p>
</section>

{# ── GRID 3 × CaseCard (DS: dark = index % 3 === 1) ── #}
<section class="ds-sec" itemscope itemtype="https://schema.org/CollectionPage">
  <meta itemprop="name" content="{{ title }}">
  <meta itemprop="description" content="{{ description }}">
  <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:20px;view-transition-name:casos-list;" class="three-col">
    {% for caso in casosPage %}
    {% set isDark = "case-card--dark" if (loop.index0 % 3 == 1) else "" %}
    <a href="/casos-exito/{{ caso.slug }}/" class="case-card {{ isDark }}" itemprop="hasPart" itemscope itemtype="https://schema.org/Article">
      <div class="case-card__top">
        <span class="case-card__cat" itemprop="about">{{ caso.category | default(caso.tags[0]) }}</span>
        <time datetime="{{ caso.datePublished }}" class="case-card__date">{{ caso.datePublished | replace("-", "/") | truncate(7, true, "") }}</time>
      </div>
      {% if caso.amount and caso.amount != "—" %}
      <div class="case-card__amount">{{ caso.amount }}</div>
      {% endif %}
      <div class="case-card__title" itemprop="name">{{ caso.title }}</div>
      <p class="case-card__desc" itemprop="description">{{ caso.description | truncate(120) }}</p>
      {% if caso.tags %}
      <div class="case-card__tags">
        {% for tag in caso.tags | slice(0,3) %}
        <span class="case-card__tag">{{ tag }}</span>
        {% endfor %}
      </div>
      {% endif %}
      <div class="case-card__cta">
        Leer caso
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
      </div>
    </a>
    {% endfor %}
  </div>

  {# ── Paginación ── #}
  {% if pagination.hrefs.length > 1 %}
  <nav class="casos-pager" aria-label="Paginación de casos" style="display:flex;align-items:center;justify-content:center;gap:10px;margin-top:clamp(36px,5vw,56px);font-family:var(--font-mono);font-size:var(--fs-mono-label);letter-spacing:var(--tracking-label);text-transform:uppercase;">
    {% if pagination.href.previous %}<a href="{{ pagination.href.previous }}" rel="prev" style="color:var(--text-accent);text-decoration:none;padding:8px 12px;border:1px solid var(--border-hairline);border-radius:var(--radius-xs);">← Anteriores</a>{% endif %}
    {% for pageUrl in pagination.hrefs %}
    {% set n = loop.index %}
    <a href="{{ pageUrl }}" aria-current="{{ 'page' if loop.index0 == pagination.pageNumber else 'false' }}" style="padding:8px 13px;border-radius:var(--radius-xs);text-decoration:none;border:1px solid {{ 'var(--accent)' if loop.index0 == pagination.pageNumber else 'var(--border-hairline)' }};color:{{ 'var(--accent-on)' if loop.index0 == pagination.pageNumber else 'var(--text-muted)' }};background:{{ 'var(--accent)' if loop.index0 == pagination.pageNumber else 'transparent' }};">{{ n }}</a>
    {% endfor %}
    {% if pagination.href.next %}<a href="{{ pagination.href.next }}" rel="next" style="color:var(--text-accent);text-decoration:none;padding:8px 12px;border:1px solid var(--border-hairline);border-radius:var(--radius-xs);">Siguientes →</a>{% endif %}
  </nav>
  {% endif %}
</section>

{# ── BandCTA (DS: tint) ── #}
<section class="band-cta band-cta--tint">
  <div style="max-width:var(--container-wide);margin:0 auto;">
    <div class="band-cta__eyebrow"><span class="page-hero__eyebrow-line"></span>Sin compromiso</div>
    <h2 class="band-cta__h2">¿Tiene un caso similar?</h2>
    <p class="band-cta__lede">Primera consulta sin coste para valorar si su caso tiene recorrido pericial. Respuesta en 24 horas.</p>
    <div class="band-cta__btn-wrap">
      <a href="/contacto/profesional/" class="band-cta__btn">
        Consultar mi caso
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
      </a>
    </div>
  </div>
</section>
