<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} | Perito {{ location.city }}</title>
    <meta name="description" content="Professional technical surveyor services in {{ location.city }}. Expert assessments, valuations, and technical reports.">

    {# Google Fonts #}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,wght@0,400;0,500;0,700;1,400&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    {# Inline Styles #}
    <style>
        {% include "../../assets/css/styles.css" %}
    </style>

    {# Favicon #}
    <link rel="icon" type="image/x-icon" href="/favicon.ico">

    {# Lucide Icons #}
    <script src="https://unpkg.com/lucide@latest"></script>

    {# Open Graph Tags #}
    <meta property="og:title" content="{{ title }} | Perito {{ location.city }}">
    <meta property="og:description" content="Professional technical surveyor services in {{ location.city }}. Expert assessments, valuations, and technical reports.">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://perito.barcelona{{ page.url }}">

    {# Twitter Card #}
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{{ title }} | Perito {{ location.city }}">
    <meta name="twitter:description" content="Professional technical surveyor services in {{ location.city }}. Expert assessments, valuations, and technical reports.">
</head>
<body class="min-h-screen bg-background font-sans antialiased">
    {% include "components/header.njk" %}

    <main>
        {{ content | safe }}
    </main>

    {% include "components/footer.njk" %}
    
    {# Initialize Lucide Icons #}
    <script>
        lucide.createIcons();
    </script>
    <script>
    if ('serviceWorker' in navigator) {
      window.addEventListener('load', () => {
        navigator.serviceWorker.register('/service-worker.js');
      });
    }
  </script>
</body>
</html>