<header class="hero-full">
    <div class="hero-wrapper">
        <div class="hero-container">
            <div class="hero-text">
                <span class="hero-tag">PERITO JUDICIAL EN BARCELONA</span>
                <div class="proof-header">
                    <div class="trust-badges">
                        <div class="badge-item">
                            <svg viewBox="0 0 24 24" class="badge-icon"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
                            <span>Nº1 EN PERITAJES</span>
                        </div>
                        <div class="rating-item">
                            <div class="stars">★★★★★</div>
                            <span>4.9/5 en Google</span>
                        </div>
                    </div>
                    <h2>INFORMES PERICIALES<br><strong>QUE GANAN CASOS</strong></h2>
                </div>

                <div class="cta-group">
                    <a href="#contacto" class="cta-btn">SOLICITAR INFORME</a>
                    <span class="cta-guarantee">✓ Primera consulta gratuita</span>
                </div>

                <div class="proof-metrics">
                    <div class="metric">
                        <div class="metric-icon">97%</div>
                        <div class="metric-text">
                            <strong>Casos Exitosos</strong>
                            <span>En tribunales</span>
                        </div>
                    </div>
                    <div class="metric">
                        <div class="metric-icon">24h</div>
                        <div class="metric-text">
                            <strong>Respuesta</strong>
                            <span>Garantizada</span>
                        </div>
                    </div>
                </div>

                <div class="trust-logos">
                    <img src="/assets/images/logos/logo_ECCAT.avif" alt="Col·legi de l'Enginyeria Civil de Catalunya" width="100" loading="lazy">
                    <img src="/assets/images/logos/INESE_Formación_CMYK.avif" alt="INESE" width="100" loading="lazy">
                    <img src="/assets/images/logos/upc-logo.svg" alt="Universitat politècnica de catalunya" width="100" loading="lazy">
                    <p class="trust-caption">Informes aceptados por todos los tribunales españoles</p>
                </div>
            </div>
        </div>
        <div class="hero-image">
            <picture>
                <source
                    media="(min-width: 992px)"
                    srcset="/assets/images/perito-construccion-obra-edificacion-1280.avif"
                >
                <source
                    media="(min-width: 768px)"
                    srcset="/assets/images/perito-construccion-obra-edificacion-768.avif"
                >
                <img
                    src="/assets/images/perito-construccion-obra-edificacion-640.avif"
                    alt="Perito experto realizando inspección técnica"
                    loading="eager"
                    decoding="async"
                >
            </picture>
        </div>
    </div>
</header>

<style>
.hero-full {
    min-height: 85vh; /* Reducido de 100vh a 85vh */
    padding-top: var(--header-height, 70px);
    margin-top: 0;
    background: linear-gradient(135deg, var(--color-dark) 0%, #1a2452 100%);
    color: white;
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
}

.hero-wrapper {
    position: relative;
    z-index: 2;
    width: 100%;
    min-height: calc(85vh - var(--header-height, 70px)); /* Ajustado para coincidir */
    display: flex;
    align-items: flex-start; /* Cambiado de center a flex-start */
    padding-top: 5vh; /* Añadido padding superior */
}

.hero-container {
    width: min(1400px, 95%);
    margin-left: max(5%, 2rem);
    padding: clamp(2rem, 6vh, 4rem) 0; /* Reducido el padding vertical */
}

.hero-image {
    position: absolute;
    top: 0;
    right: 0;
    width: 45%; /* Reducido de 55% a 45% */
    height: 100%;
    z-index: 1;
}

.hero-image picture {
    height: 100%;
}

.hero-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    clip-path: polygon(150px 0, 100% 0, 100% 100%, 50px 100%); /* Ajustada la diagonal */
    opacity: 0.8;
}

.hero-tag {
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: 1rem;
    display: inline-block;
    position: relative;
    padding-left: 3rem;
}

.hero-tag::before {
    content: "";
    position: absolute;
    left: 0;
    top: 50%;
    width: 2rem;
    height: 2px;
    background-color: var(--color-accent);
    transform: translateY(-50%);
}


@media (max-width: 991px) {
    .hero-full {
        min-height: 85vh;
        padding-top: var(--header-height, 70px);
    }

    .hero-wrapper {
        min-height: auto;
        padding: rem 0;
        flex-direction: column;
        align-items: center;
        padding-top: 2vh;
    }

    .hero-container {
        width: min(650px, 95%);
        margin: 0 auto;
        padding: 0;
    }

    .hero-image {
        position: relative;
        width: 100%;
        height: 300px;
    }

    .hero-image img {
        clip-path: none;
        opacity: 0.8;
    }

    .hero-tag {
        padding: 0;
        margin-bottom: 0rem;
    }

    .hero-tag::before {
        display: none;
    }
}

@media (max-width: 768px) {
    .hero-wrapper {
        padding: 0rem 0;
        min-height: auto; /* Permite altura automática en móvil */
    }

    .hero-container {
        padding-top: 1rem;
    }

    .hero-image {
        height: 300px;
    }

    h2 {
        font-size: clamp(2rem, 3vw + 1rem, 2.5rem);
    }
}

@media (min-width: 992px) and (max-width: 1200px) {
    .hero-container {
        max-width: 50%; /* Reducir el ancho del contenedor */
        margin-left: max(4%, 2rem);
    }

    .hero-image {
        width: 48%; /* Ajustar el ancho de la imagen */
    }

    h2 {
        font-size: clamp(2.25rem, 3vw + 1rem, 3.5rem); /* Reducir tamaño de título */
    }

    .trust-badges {
        gap: 0.75rem; /* Reducir espaciado */
    }
}

.proof-header {
    margin-bottom: 1rem;
}

.trust-badges {
    display: flex;
    gap: 1rem;
    margin-bottom: 1.5rem;
}

.badge-item, .rating-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.625rem 1rem;
    background: rgba(255,255,255,0.1);
    border-radius: 100px;
    font-size: 1rem;
}

.badge-icon {
    width: 20px;
    height: 20px;
    fill: var(--color-accent);
}

.stars {
    color: #FFD700;
    letter-spacing: 2px;
}

h2 {
    font-size: clamp(2.5rem, 4vw + 1rem, 4.5rem);
    line-height: 1.1;
    letter-spacing: -0.02em;
    margin-bottom: 1rem;
}

h2 strong {
    display: block;
    color: var(--color-accent);
    font-size: 1.1em;
}

.proof-metrics {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.75rem;
    margin: 1rem 0; /* Reducido el margen */
    order: 4; /* Movido después del CTA */
}

.metric {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    background: rgba(255,255,255,0.05);
    border-radius: 12px;
}

.metric-icon {
    font-size: clamp(2rem, 3vw, 2.5rem);
    font-weight: 800;
    color: var(--color-accent);
    line-height: 1;
}

.metric-text {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
}

.metric-text strong {
    font-size: clamp(1rem, 1.5vw, 1.25rem);
    color: white;
}

.metric-text span {
    font-size: 0.875rem;
    color: rgba(255,255,255,0.7);
}

.cta-group {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: flex-start;
    order: 3; /* Movido antes de proof-metrics */
    margin-top: 1rem; /* Añadido margen superior */
}

.cta-guarantee {
    color: var(--color-accent);
    font-size: 0.875rem;
}

.trust-logos {
    display: flex;
    flex-wrap: wrap;
    gap: 2rem;
    align-items: center;
    margin-top: 2.5rem;
    padding-top: 2rem;
    border-top: 1px solid rgba(255,255,255,0.1);
    order: 5;
    position: relative;
    justify-content: space-between;
}

.trust-logos img {
    height: 40px;
    width: auto;
    max-width: 120px;
    filter: brightness(0) invert(1);
    opacity: 0.7;
    transition: all 0.3s ease;
    object-fit: contain;
}

.trust-logos img:hover {
    opacity: 1;
    transform: translateY(-2px);
}

.trust-caption {
    color: rgba(255,255,255,0.6);
    font-size: 0.875rem;
    margin: 0;
    flex: 1;
    min-width: 200px;
    text-align: right;
}

@media (max-width: 991px) {
    .hero-container {
        grid-template-columns: 1fr;
    }

    .hero-text {
        text-align: center;
        align-items: center;
    }

    h2 {
        font-size: clamp(2.25rem, 3vw + 1rem, 3rem);
        text-align: center;
    }

    .trust-badges, .proof-metrics {
        justify-content: center;
    }

    .proof-metrics {
        grid-template-columns: repeat(2, 1fr);
    }

    .cta-group {
        align-items: center;
    }

    .trust-logos {
        justify-content: center;
        gap: 1.5rem;
    }

    .trust-caption {
        text-align: center;
        margin-top: 1.5rem;
        flex: 0 0 100%;
    }
}

@media (max-width: 768px) {
    .hero-container {
        padding: 2rem 1rem;
    }

    .trust-badges {
        flex-direction: column;
        align-items: center;
    }

    .proof-metrics {
        grid-template-columns: 1fr;
    }

    .metric {
        padding: 0.875rem;
        justify-content: center;
    }
}

.mobile-menu-container {
    padding-top: calc(var(--header-height, 70px) + 2rem); /* Ajustado el padding superior */
    padding-bottom: 32px;
    min-height: 100%;
    display: flex;
    flex-direction: column;
}

.mobile-nav-list {
    list-style: none;
    padding: 1rem;
    margin: 0 0 30px 0;
}

@media (max-width: 991px) {
    .mobile-menu {
        padding-top: var(--header-height, 70px); /* Añadido padding superior */
    }
    
    .mobile-menu-container {
        padding-top: 1rem; /* Reducido porque ya tenemos padding en el contenedor padre */
    }
}

.hero-conversion {
    min-height: 100vh;
    background: linear-gradient(135deg, var(--color-dark) 0%, #1a2452 100%);
    color: white;
    padding: clamp(5rem, 10vh, 8rem) 1rem;
    display: flex;
    align-items: center;
    overflow: hidden;
}

.hero-content {
    width: min(1400px, 100%);
    margin: 0 auto;
    display: grid;
    gap: 3rem;
    align-items: center;
    padding: 0 max(5%, 1rem);
}

.hero-text {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    max-width: 850px;
}

.hero-badge {
    display: inline-flex;
    padding: 0.75rem 1.25rem;
    background: rgba(0, 184, 212, 0.1);
    border: 1px solid rgba(0, 184, 212, 0.2);
    border-radius: 100px;
    font-size: 0.875rem;
    font-weight: 700;
    color: var(--color-accent);
    margin-bottom: 1rem;
}

.hero-title {
    font-size: clamp(2.5rem, 5vw + 1rem, 4rem);
    line-height: 1.1;
    font-weight: 700;
    letter-spacing: -0.02em;
}

.hero-title strong {
    display: block;
    color: var(--color-accent);
    font-size: 1.1em;
    margin-top: 0.2em;
}

.hero-stats {
    display: flex;
    gap: 2rem;
    margin: 0.5rem 0;
}

.stat-item {
    font-size: 1.125rem;
    color: rgba(255,255,255,0.9);
}

.stat-item strong {
    display: block;
    font-size: 2rem;
    font-weight: 800;
    color: var(--color-accent);
    line-height: 1;
    margin-bottom: 0.25rem;
}

.hero-description {
    font-size: 1.25rem;
    line-height: 1.6;
    color: rgba(255,255,255,0.9);
    margin: 0.5rem 0 1.5rem;
}

.cta-group {
    display: flex;
    flex-direction: column;
    gap: 1rem;
}

.cta-primary {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1.25rem 2rem;
    background: linear-gradient(45deg, var(--color-accent), #00d4ff);
    border-radius: 8px;
    color: white;
    font-weight: 700;
    text-decoration: none;
    transition: all 0.3s ease;
    width: fit-content;
}

.cta-primary svg {
    width: 20px;
    height: 20px;
    stroke: currentColor;
    stroke-width: 2;
    fill: none;
}

.cta-guarantee {
    font-size: 0.875rem;
    color: var(--color-accent);
}

.hero-visual {
    position: relative;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
}

.hero-visual img {
    width: 100%;
    height: auto;
    display: block;
}

@media (min-width: 992px) {
    .hero-content {
        grid-template-columns: 1.2fr 0.8fr;
    }

    .hero-visual {
        height: 100%;
    }

    .hero-visual img {
        height: 100%;
        object-fit: cover;
    }
}

@media (max-width: 768px) {
    .hero-conversion {
        padding: 6rem 0 3rem;
        min-height: auto;
    }

    .hero-text {
        text-align: center;
        margin: 0 auto;
    }

    .hero-badge {
        margin: 0 auto;
    }

    .hero-stats {
        justify-content: center;
    }

    .cta-group {
        align-items: center;
    }

    .hero-visual {
        margin: 0 -1rem;
        border-radius: 0;
    }
}
</style>