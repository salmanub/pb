<section class="py-20 px-4 md:px-[5%] bg-gradient-to-br from-slate-50 via-white to-slate-100 relative" itemprop="mainEntity" itemscope itemtype="https://schema.org/CollectionPage">
    {# Pattern Background #}
    <div class="absolute inset-0 bg-grid-pattern opacity-[0.15]"></div>

    {# Contenido Principal #}
    <div class="relative z-10 max-w-[1400px] mx-auto">
        {# Header de la sección mejorado #}
        <div class="text-center mb-16">
            <div class="inline-flex flex-col items-center">
                <span class="inline-flex justify-center items-center text-sm md:text-base font-semibold tracking-[2px] uppercase text-cyan-600 mb-6">
                    <span class="w-8 h-0.5 bg-cyan-500/50 mr-3"></span>
                    CASOS DE ÉXITO
                    <span class="w-8 h-0.5 bg-cyan-500/50 ml-3"></span>
                </span>
                <h2 class="text-[2.5rem] md:text-[3.5rem] font-light leading-[1.1] tracking-tight" itemprop="headline">
                    <span class="text-slate-800">Descubra Nuestros</span>
                    <span class="block mt-4 bg-gradient-to-r from-cyan-600 to-cyan-500 bg-clip-text text-transparent">
                        <strong>Casos de Éxito</strong>
                    </span>
                </h2>
                <p class="mt-6 text-xl text-slate-600 max-w-3xl">
                    Ejemplos reales de cómo nuestros informes periciales han ayudado a resolver casos complejos
                </p>
            </div>
        </div>

        {# Grid de Casos #}
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-16">
            {% if casos.destacados %}
                {% for destacado in casos.destacados %}
                    {% if loop.index0 < 2 %}
                    <div class="bg-white rounded-xl shadow-lg hover:shadow-2xl transition-all duration-300 overflow-hidden flex flex-col group hover:-translate-y-1" itemscope itemtype="https://schema.org/Article">
                        {% if destacado.image %}
                        <img src="{{ destacado.image }}" 
                             alt="{{ destacado.imageAlt }}" 
                             class="w-full h-48 object-cover group-hover:scale-105 transition-transform duration-500"
                             itemprop="image">
                        {% endif %}
                        <div class="p-6 flex flex-col flex-grow">
                            <div class="mb-4 flex flex-wrap gap-2">
                                {% for tag in destacado.tags %}
                                <span class="tag-categoria text-xs font-semibold px-3 py-1 rounded-full
                                           {% if tag == 'Construcción' %}bg-cyan-100 text-cyan-700
                                           {% elif tag == 'Seguros' %}bg-green-100 text-green-700
                                           {% else %}bg-slate-100 text-slate-700{% endif %}"
                                      itemprop="keywords">
                                    {{ tag }}
                                </span>
                                {% endfor %}
                            </div>
                            <h3 class="text-xl font-bold text-slate-800 mb-3" itemprop="headline">{{ destacado.title }}</h3>
                            <p class="text-slate-600 text-sm leading-relaxed mb-4 flex-grow" itemprop="description">{{ destacado.excerpt }}</p>
                            <div class="font-semibold text-green-600 mb-4" itemprop="resultDescription">{{ destacado.result }}</div>
                            <a href="{{ destacado.url }}" 
                               class="mt-auto inline-flex items-center text-cyan-600 hover:text-cyan-700 font-semibold group"
                               itemprop="url">
                                Ver detalles del caso {{ destacado.title | lower }}
                                <svg class="w-4 h-4 ml-2 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
                                </svg>
                            </a>
                        </div>
                    </div>
                    {% endif %}
                {% endfor %}
            {% endif %}

            {# Card de "Ver más" con gradiente similar al hero #}
            <div class="bg-gradient-to-br from-[#0B1030] to-[#1a2452] rounded-xl shadow-lg hover:shadow-2xl transition-all duration-300 overflow-hidden flex flex-col items-center justify-center p-8 text-center group hover:-translate-y-1">
                <div class="w-16 h-16 rounded-full bg-cyan-500/20 flex items-center justify-center mb-6 group-hover:scale-110 transition-transform">
                    <svg class="w-8 h-8 text-cyan-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/>
                    </svg>
                </div>
                <h3 class="text-2xl font-light text-white mb-4">Ver Más Casos de Éxito</h3>
                <p class="text-cyan-100/80 text-lg mb-8">Descubre cómo hemos ayudado a cientos de clientes a resolver sus problemas técnicos y legales.</p>
                <a href="/casos-exito/" class="inline-flex items-center px-8 py-4 bg-cyan-500 text-white font-semibold rounded-lg hover:bg-cyan-600 transition-all group">
                    Explorar todos los casos
                    <svg class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
                    </svg>
                </a>
            </div>
        </div>

        {# CTA Adicional #}
        <div class="flex justify-center mt-8">
            <a href="/casos-exito/" class="inline-flex items-center text-slate-600 hover:text-cyan-600 font-medium transition-colors group">
                Ver todos los casos de éxito
                <svg class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
                </svg>
            </a>
        </div>
    </div>
</section>
